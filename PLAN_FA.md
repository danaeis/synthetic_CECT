# طرح پژوهش: سنتز CECT از NCCT — تشخیص گلوگاه و مسیر پیش‌رو

سند فارسی پروژه. نسخهٔ کامل انگلیسی: `PROJECT_PLAN.md` (نقشهٔ راه و نتایج) و
`IMPLEMENTATION.md` (آنچه کد واقعاً انجام می‌دهد).

نام متریک‌ها، مسیر فایل‌ها، فلگ‌ها و شناسه‌های کد عمداً به انگلیسی مانده‌اند تا با
خود کد یکی باشند.

> آموزش روی یک ماشین GPU از راه دور اجرا می‌شود. این نسخه فقط آینهٔ کد و نتایج
> است: نه داده دارد، نه CUDA، نه checkpoint. **هرگز `best_model.pth` را پاک نکنید**
> — شش اجرای seed اولیه دیگر قابل بازتولید نیستند چون checkpoint آن‌ها از بین رفته.

---

## ۱. مسئله در یک نگاه

خطای per-organ HU روی حدود **۸ تا ۹ HU** متوقف شده و هیچ تغییری در loss یا معماری
آن را تکان نداده است. سؤال اصلی این بود: **چرا؟**

پاسخ با اندازه‌گیری به دست آمد، نه با حدس.

---

## ۲. زنجیرهٔ تشخیص — چه چیزهایی رد شدند

هر ردیف با یک اندازه‌گیری مستقل رد شده است:

| فرضیه | وضعیت | شاهد |
|---|---|---|
| **Overfitting** | رد شد | فاصلهٔ train/val فقط ۶٪ است: train 8.32 HU در برابر val 8.86 HU |
| **خطای Registration** | رد شد | همبستگی ناچیز (\|r\| ≤ 0.25). مهم‌تر: بدترین کیس از نظر registration خطای متوسط دارد، و دو بدترین کیس از نظر featHU هر دو **بهتر از میانگین** registration دارند |
| **ناهمگونی مقیاس voxel** | رد شد | هر ۲۷۴ حجم دقیقاً ۱.۵×۱.۵×۱.۵ میلی‌متر هستند (`scripts/spacing_stats.py`) |
| **ظرفیت مدل** | رد شد | ۹ برابر کردن پارامترها (۳.۳M → ۳۰.۰M) فقط ۰.۳۳ HU سود داد |
| **کمبود داده** | رد شد | منحنی یادگیری اشباع شده — برون‌یابی به ~۸.۷۴ HU می‌رسد در برابر ۸.۸۶ فعلی |

### منحنی یادگیری (پاسخ به «آیا داده بیشتر لازم است؟»)

| تعداد کیس | ۱۰ | ۲۵ | ۵۰ | ۹۷ |
|---|---|---|---|---|
| val HU | 9.97 | 9.31 | 8.98 | **8.86** |
| سود | — | −0.66 | −0.33 | −0.12 |

هر بار دو برابر شدن داده، نصف سود قبلی را می‌دهد. یعنی **حتی داده بی‌نهایت** از
همین نوع فقط ۰.۱۲ HU دیگر می‌دهد. افزودن دیتاست جدید اهرم درستی نیست.

---

## ۳. یافتهٔ اصلی: خطا Aleatoric است

`scripts/audit_enhancement.py` سه پیش‌بینی فرضیه را همزمان آزمود:

| آماره | مقدار | معنا |
|---|---|---|
| میانهٔ β (اندام‌های کنتراستی) | **۰.۱۸۱** | مدل فقط **۱۸٪** از تغییرات سطح کنتراست بین کیس‌ها را بازیابی می‌کند |
| میانهٔ var(gen)/var(real) | **۰.۱۷۶** | خروجی **۵.۷ برابر کم‌پراکنده** است |
| همبستگی featHU با سطح واقعی aorta | **r = +۰.۷۶۵** | **۵۹٪** خطای هر کیس فقط با سطح کنتراست همان کیس توضیح داده می‌شود |

**علامت مثبت همبستگی، دلیل قاطع است**: هرچه کیس کنتراست بیشتری داشته باشد، خطا
بیشتر است. این دقیقاً امضای *بازگشت به میانگین* است — مدل تقریباً همیشه سطح
«متوسط» را تولید می‌کند، پس کیس‌های پرکنتراست را کم‌برآورد و کیس‌های کم‌کنتراست را
بیش‌برآورد می‌کند. سوگیری portal vein برابر **−۳۶ HU** است.

**کنترل‌های منفی درست رفتار کردند**: کیسهٔ صفرا (r=+۰.۲۴۲) و کولون (r=+۰.۱۲۷) که
تقریباً کنتراست نمی‌گیرند، این اثر را نشان نمی‌دهند. این احتمالِ «کیس‌های سخت
صرفاً سخت‌اند» را رد می‌کند.

**این یافته وابسته به loss نیست.** روی دو مدل کاملاً متفاوت تکرار شد:

| اندام | β در curriculum | β در adv_organ |
|---|---|---|
| aorta | ۰.۲۳۵ | **۰.۲۳۴** |
| liver | ۰.۳۲۰ | **۰.۳۲۹** |
| colon | ۰.۲۵۷ | **۰.۲۶۱** |

پس این یک ویژگی **خودِ مسئله** است، نه یک ویژگی loss.

### چرا؟

سطح مطلق کنتراست به **دوز تزریق، زمان‌بندی بولوس، برون‌ده قلبی و فیزیولوژی بیمار**
بستگی دارد — و **هیچ‌کدام از این‌ها در یک اسکن بدون کنتراست دیده نمی‌شوند**.

اگر مسئله را این‌طور بنویسیم:

```
CECT  =  f( NCCT , Z )        Z = دوز، زمان‌بندی بولوس، فیزیولوژی
```

`Z` نه در ورودی هست و نه از آن قابل بازیابی. بنابراین بهترین کاری که هر مدل
قطعی (deterministic) می‌تواند بکند `E[CECT | NCCT]` است — یعنی میانگین شرطی — و خطای
تقلیل‌ناپذیر برابر `E[Var(CECT | NCCT)]` است. اندازه‌گیری‌های بالا می‌گویند مدل
**همین حالا روی همان سقف است**.

---

## ۴. چرا این معماری، و چرا nnU-Net یا SwinUNet یا CycleGAN نه

نکتهٔ کلیدی: **شرطی‌سازی (conditioning) یک تغییر معماری نیست، یک تغییر اطلاعات
است.** همهٔ گزینه‌های دیگر *نحوهٔ تقریب تابع f* را عوض می‌کنند؛ هیچ‌کدام *اطلاعات
موجود در ورودی* را عوض نمی‌کنند. اگر خروجی توسط ورودی تعیین نشود، تقریب‌زنندهٔ
بهتر کمکی نمی‌کند.

| رویکرد | چه چیزی را بهتر می‌کند | آیا به گلوگاه ما می‌خورد؟ |
|---|---|---|
| **nnU-Net** | پیش‌پردازش خودکار، augmentation، ensembling پنج‌تایی | **نه.** منطق resampling آن روی دادهٔ یکنواخت ۱.۵mm بی‌اثر است، decoder آن logit کلاس می‌دهد، و **ensembling میانگین می‌گیرد** — یعنی خروجی را باز هم به میانگین شرطی نزدیک‌تر و کم‌پراکندگی را **بدتر** می‌کند |
| **SwinUNet / TransUNet / Restormer** | استدلال فضایی بلندبرد | **نه.** خطای ما یک خطای **سطح (DC) per-organ** است، نه خطای ساختار فضایی. `norm_attribution` نشان داد ۶۴.۵٪ از seam مدل آموزش‌دیده کاملاً DC است |
| **CycleGAN** | حذف نیاز به دادهٔ جفت‌شده | **نه، و اینجا فعالانه اشتباه است.** ما دادهٔ جفت‌شده **داریم** — استفاده از آن یعنی دور ریختن نظارت. بدتر: قید cycle، نگاشت را به سمت قطعی و وارون‌پذیر بودن می‌راند، که دقیقاً bias نادرست برای یک نگاشت واقعاً یک‌به‌چند است |
| **شرطی‌سازی FiLM** | بخشی از `Z` را قابل مشاهده می‌کند | **بله** — تنها گزینه‌ای که به جای تقریب `f`، به `Var(Y\|X)` حمله می‌کند |

**دفاع منصفانه از GAN** — تنها استدلال متقابل واقعی: یک هدف adversarial *می‌تواند*
توزیع را مدل کند و کم‌پراکندگی را درمان کند. اما این شاخه قبلاً اجرا شده و نکرد:

| | var ratio در aorta | featHU |
|---|---|---|
| `l1_organ_curriculum` | ۰.۱۲ | ۱۵.۷۴ |
| `l1_adv_organ` | ۰.۱۲ | ۱۶.۰۴ |

adversarial پراکندگی اندام‌های کنتراستی را اصلاً تغییر نداد. اما بافت را به‌شدت
بهتر کرد — که موضوع بخش بعدی است.

---

## ۵. تاری تصویر در برابر خطای سطح: دو نشانه از یک علت

ضرر L1 که به میانهٔ شرطی همگرا می‌شود **دو** نشانه تولید می‌کند:

1. **از دست رفتن جزئیات فرکانس‌بالا** (تاری تصویر)
2. **کم‌پراکندگی سطح** (خطای HU)

اندازه‌گیری‌ها این دو را از هم جدا می‌کنند:

| مدل | gradW1 | oGradW1 | raps_hf (هدف ۱.۰) | featHU |
|---|---|---|---|---|
| `l1_organ_curriculum` | ۰.۰۰۱۹ | ۰.۰۰۶۲ | ۰.۸۴۰ | **۱۵.۷۴** |
| `l1_adv_organ` | **۰.۰۰۰۹** | **۰.۰۰۲۶** | **۰.۹۰۵** | ۱۶.۰۴ |

adversarial روی gradW1 **۲.۱ برابر**، روی oGradW1 **۲.۴ برابر** و روی raps_hf
**۱.۷ برابر** نزدیک‌تر به ۱.۰ است — با هزینهٔ **+۰.۳۰ HU در featHU که داخل بازهٔ
نویز ۰.۸۲ HU است**، یعنی عملاً رایگان.

اما پراکندگی سطح را دست‌نخورده گذاشت (var ratio هر دو ۰.۱۲).

**نتیجه: این دو مداخله مکمل‌اند، نه رقیب.** adversarial مؤلفهٔ فرکانس‌بالا را
درست می‌کند، شرطی‌سازی سطح مؤلفهٔ DC را. برای همین هر دو در طرح نهایی هستند.

### دو تغییر لازم در Discriminator

1. **شرطی شود**: به‌جای تصویر تنها، `cat([source, image])` ببیند — همان `D(x, y)`
   واقعی pix2pix. حالت فعلی یک منتقد بافت است که نمی‌داند تصویر از کدام NCCT آمده.
   این احتمالاً بی‌ثباتی مشاهده‌شده را هم درمان می‌کند (`train_adv` از ۰.۵۲ به ۰.۹۶
   می‌رفت در حالی که `train_disc` از ۰.۲۴۵ به ۰.۱۷۸ می‌آمد — یعنی D داشت می‌برد).
2. **بردار شرطی‌سازی را ببیند**: وقتی به generator گفته می‌شود «arterial»، یک D که
   عمدتاً آمار venous یاد گرفته، خروجی **درستِ** arterial را جریمه می‌کند.

⚠ بردار شرطی‌سازی داده‌شده به D همیشه `.detach()` می‌شود — هم چون backward مربوط به
D گراف generator را آزاد می‌کند، هم چون در غیر این صورت G می‌توانست به‌جای بهتر کردن
تصویر، با تغییر «آنچه ادعا می‌کند درخواست شده» ضرر adversarial را کم کند.

---

## ۶. نتیجهٔ تأییدشده: GroupNorm

با سه seed در برابر `l1_organ_curriculum_s{42,43,44}`:

| متریک | curriculum | groupnorm | آستانهٔ ۲σ | Δ | حکم |
|---|---|---|---|---|---|
| **feature_l1_hu** | ۱۴.۵۰۱ | **۱۳.۴۶۱** | ۰.۸۳۸ | **−۱.۰۴۰** | **واقعی** |
| org_mae | ۰.۰۲۹۸ | **۰.۰۲۸۷** | ۰.۰۰۰۲ | −۰.۰۰۱۱ | واقعی |
| org_ssim | ۰.۹۶۸۳ | **۰.۹۶۹۸** | ۰.۰۰۰۲ | +۰.۰۰۱۵ | واقعی |
| psnr | ۳۰.۳۷۴ | **۳۰.۶۳۲** | ۰.۰۸۷ | +۰.۲۵۸ | واقعی |
| **seam** | ۱.۳۴۶۱ | ۱.۳۵۴۱ | ۰.۰۰۷۷ | **+۰.۰۰۸۰** | **واقعی — ولی بدتر** |

−۱.۰۴ HU **بزرگ‌ترین بردِ تأییدشدهٔ تک‌فلگ در کل پروژه** است.

**اما نیمی از پیش‌بینی شکست خورد.** قرار بود GroupNorm هر دو نشانهٔ یک علت را درمان
کند؛ نشانهٔ HU را درمان کرد و نشانهٔ tiling را نه. دلیلش از قبل در `scripts/erf.py`
قابل دیدن بود: شعاع پشتیبانی گرادیان در group norm برابر **۱۲۸ = patch/2 و همچنان
بی‌کران** است، دقیقاً مثل instance norm. فقط batch norm در ۹۴ پیکسل کران‌دار بود.
یعنی `norm_attribution.py` و اندازه‌گیری ERF با هم اختلاف داشتند و **در مورد seam،
ERF درست می‌گفت**.

این را باید صادقانه گزارش کرد: یک پیش‌بینی مکانیستیک که نیمی درست بود، همراه با
اندازه‌گیری‌ای که از قبل هشدار داده بود. ضمناً آزمایش بعدی را مشخص می‌کند: اگر seam
مهم است، `--generator_norm batch` تنها گزینه‌ای است که ERF می‌گوید ذاتاً
tile-invariant است.

---

## ۷. آزمایش‌های آماده برای اجرا

### ۷.۱ شرطی‌سازی پیوستهٔ سطح

یک مدل آموزش می‌بیند و **سه‌گونه** ارزیابی می‌شود:

| حالت | ورودی در زمان استنتاج | چه چیزی را می‌سنجد |
|---|---|---|
| `oracle` | سطح واقعی همان کیس از CECT واقعی | **سقف** — خطا اگر سطح معلوم بود |
| `population` | میانگین مجموعهٔ train برای همه | باید baseline بدون شرط را بازتولید کند |
| `fixed` | هر مقداری که پزشک بخواهد | حالت قابل استقرار |

> **نتیجهٔ قابل گزارش، «فاصلهٔ oracle منهای population» است — نه عدد oracle به
> تنهایی.** حالت oracle پاسخ را از روی CECT واقعی می‌خواند و هرگز یک متریک اصلی
> نیست. آن فاصله دقیقاً اندازهٔ مؤلفهٔ aleatoric است.

**هشدار دورِ باطل**: خودِ `featHU` میانگین خطای میانهٔ HU هر اندام است، پس
شرطی‌سازی روی میانهٔ اندام‌ها تا حدی یعنی دادن پاسخ. برای همین ablation زیر طراحی
شده تا این آلودگی **دیده شود** نه پنهان بماند، و همیشه باید
`scripts/heldout_feathu.py` هم اجرا شود که featHU را فقط روی اندام‌های
**کنارگذاشته‌شده** حساب می‌کند.

| بازو | scenario | شرطی‌سازی | اندام‌های کنارگذاشته |
|---|---|---|---|
| **B0** | `b0_groupnorm_adv` | ندارد — baseline بافت | ۱۶ |
| **L1** | `level_aorta` | aorta (۱ عدد) | ۱۵ |
| **L2** | `level_aorta_pv` | aorta + portal vein (۲) | ۱۴ |
| **L3** | `level_all8` | هر ۸ اندام | ۸ |

اگر **L1 به‌تنهایی** بیشتر خطا را بازیابی کند، سطح کنتراست یک **عامل نهفتهٔ سراسری
واحد** است — یافته‌ای تمیز و قابل دفاع. اگر بازیابی با هر عدد اضافه رشد کند، هر
اندام تغییرات پیش‌بینی‌ناپذیر مستقل دارد. هر دو حالت یک نتیجه‌اند.

```bash
python scripts/dump_levels.py                  # -> splits/levels.json (اطلاعات ORACLE)
python scripts/probe_level_predictability.py   # آیا سطح اصلاً از NCCT قابل پیش‌بینی است؟
./run_scenarios.sh b0_groupnorm_adv level_aorta level_aorta_pv level_all8
```

### ۷.۲ آزمون پیش‌بینی‌پذیری — مستقیم‌ترین شاهد

`scripts/probe_level_predictability.py` سطح CECT هر کیس را از روی ویژگی‌های
**NCCT خودش** رگرسیون می‌کند:

- **R² ≈ ۰** → NCCT هیچ اطلاعاتی دربارهٔ میزان کنتراست نهایی ندارد. این یعنی
  عدم‌قطعیت aleatoric **مستقیماً اندازه‌گیری شده**، نه از راه حذف گزینه‌های دیگر
  استنتاج شده. قوی‌ترین تک‌شاهد کل رساله.
- **R² بالا** → سطح قابل پیش‌بینی است و مدل صرفاً یاد نمی‌گیرد؛ یعنی این یک شکست
  **مدل‌سازی** است نه سقف داده، و کل نقشهٔ راه باید عوض شود.

### ۷.۳ ورودی ۲.۵ بعدی — تنها کمبود هندسی باقی‌مانده

فاصله‌گذاری یکنواخت ۱.۵mm است و یک patch ۱۲۸ پیکسلی همین حالا **۱۹۲ میلی‌متر**
درون‌صفحه‌ای را پوشش می‌دهد که برای مقطع هر اندامی کافی است. اما `patch_depth=1`
به مدل فقط **۱.۵ میلی‌متر** از یک aorta که **۲۵۸ میلی‌متر** امتداد دارد نشان می‌دهد.

`2k+1` برش مجاور به‌عنوان **کانال** داده می‌شود و برش مرکزی پیش‌بینی می‌شود — شبکه
دوبعدی می‌ماند و پارامتر اضافه تقریباً صفر است.

```bash
./run_scenarios.sh slices5_k2 slices11_k5      # k=2 → ۷.۵mm ، k=5 → ۱۶.۵mm
```

پنجرهٔ z با **edge-clamp** ساخته می‌شود نه با کوچک کردن بازه: در زمان استنتاج
به‌هرحال باید برش ۰ و برش آخر تولید شوند، پس clamp اجتناب‌ناپذیر است و استفاده از
همان قاعده در آموزش، ناهماهنگی train/test در لبه‌ها را از بین می‌برد.

---

## ۸. مدل‌های Diffusion — طرح تصمیم‌گیری

سه دروازه، از ارزان به گران. **قبل از دروازهٔ اول هیچ کار مدل‌سازی شروع نشود.**

### D1 — آزمون وفاداری VAE (حدود یک ساعت، همه چیز را تعیین می‌کند)

Stable Diffusion در فضای نهفتهٔ یک VAE کار می‌کند که روی تصاویر طبیعی RGB با
downsampling ۸ برابر آموزش دیده. CECT‌های واقعی را از این VAE عبور دهید
(بدون هیچ diffusion و هیچ آموزشی) و featHU بازسازی را اندازه بگیرید.

**اگر featHU همین رفت‌وبرگشت از ~۸ HU بیشتر شود، هر مسیر مبتنی بر SD از همان ابتدا
مرده است** — یعنی autoencoder بیش از کل خطای مدل فعلی شما وفاداری HU را از بین
می‌برد و هیچ fine-tuning روی بخش diffusion آن را برنمی‌گرداند.

انتظار می‌رود شکست بخورد: VAE مربوطه برای کیفیت ادراکی روی RGB هشت‌بیتی بهینه شده،
نه برای حفظ یک پنجرهٔ ۶۰۰ HU در یک کانال.

### D2 — آیا diffusion اصلاً پاسخ مسئلهٔ ماست؟

diffusion توزیع `p(y|x)` را مدل می‌کند نه `E[y|x]` — که از نظر مفهومی پاسخ درستی به
یک باقی‌ماندهٔ aleatoric است. اما باید دقیق بود:

- **یک نمونه** یعنی یک زمان‌بندی بولوس **تصادفی** — برای قرائت per-organ HU احتمالاً
  بدتر از میانگین است
- **میانگین N نمونه** شما را به همان میانگین شرطی برمی‌گرداند، یعنی دقیقاً همان‌جا
  که الان هستید
- استفادهٔ صادقانه: **N نمونه بگیر، میانگین و پراکندگی را گزارش کن** → عدم‌قطعیت
  کالیبره‌شده

پس ارزش diffusion اینجا **کمّی‌سازی عدم‌قطعیت است، نه دقت**. این یک مشارکت علمی
معتبر است — اما رگرسیون heteroscedastic (پیش‌بینی μ و σ با Gaussian NLL) همان
فایده را با حدود **۱٪ محاسبات** می‌دهد.

### D3 — از صفر یا از مدل از پیش آموزش‌دیده، و loss‌های موجود

فقط اگر D1 و D2 رد شوند.

| مسیر | حکم |
|---|---|
| **DDPM شرطی در فضای پیکسل، ۱۲۸×۱۲۸، از صفر** | تنها مسیری که احتمالاً D1 باز می‌گذارد. ۹۷ کیس برای diffusion کم است؛ انتظار underfitting داشته باشید و برای seed بودجه بگذارید چون نمونه‌گیری واریانس خودش را اضافه می‌کند |
| **fine-tune کردن Stable Diffusion** | مشروط به D1. ضمناً فاصلهٔ دامنه بسیار بزرگ است: RGB طبیعی → تک‌کانال HU |
| **ControlNet روی SD** | همان محدودیت VAE؛ شاخهٔ کنترل، decoder را درست نمی‌کند |

**loss‌های موجود شما قابل انتقال‌اند.** یک مدل diffusion می‌تواند loss کمکی روی
`x0` پیش‌بینی‌شده (تخمین denoise‌شده) در هر گام داشته باشد، پس
`OrganWeightedLoss` و `OrganHUProfileLoss` از `losses.py` مستقیماً قابل استفاده‌اند.

**پیش‌بینی صادقانهٔ نتیجه، با توجه به همهٔ اندازه‌گیری‌ها**: featHU قابل‌مقایسه، به‌علاوهٔ
تخمین عدم‌قطعیت. diffusion نحوهٔ تقریب `f` را عوض می‌کند؛ بخش ۳ می‌گوید گلوگاه،
تقریب `f` نیست.

---

## ۹. قواعد خواندن نتایج بدون فریب خوردن

- **هرگز فقط با PSNR/SSIM قضاوت نکنید.** این دو در بازه‌ای صاف بودند که یک اثر با
  t=−۴.۲۲ در آن وجود داشت.
- **هر تفاوتی را در برابر کف نویز بسنجید.** نمونهٔ عبرت‌آموز: یک نتیجهٔ p<0.001 با
  سه seed **ناپدید شد و علامتش برعکس شد**.
- **از آزمون‌های زوجی per-case استفاده کنید.** واریانس بین‌کیسی (۶.۲ HU) بر تفاوت
  بین‌مدلی (۳.۲ HU) غلبه دارد.
- **انتظار داشته باشید PSNR روی یک مدل خوب کاهش یابد.** این را از قبل بگویید.
- **`run_config.json` را با اجرای قبلی diff بگیرید** — فقط باید کلیدهایی که قصد
  داشتید تغییر کرده باشند. ارزان‌ترین محافظ در برابر confound.

---

## ۱۰. ریسک‌های باز

- **مجموعهٔ تست با n=۲۰ نازک است.** هزینهٔ مشخصش: در n=۲۰ فقط \|r\| > ۰.۴۴ معنادار
  است، پس ممیزی registration نتوانست «بی‌اثر» را از «کم‌توان» تفکیک کند.
- **`phase_acc` اشباع شده** (۱.۰۰۰ برای هر شش اجرای seed) — این متریک دیگر توان
  تفکیک مدل‌ها را ندارد. ادعا باید روی `feature_l1_hu` و `org_mae` بایستد.
- **ماسکی که shape آن با حجم نخواند، بی‌صدا صفر می‌شود**
  (`dataset.py:452`) و organ-focus بی‌صدا هیچ کاندیدی پیدا نمی‌کند
  (`dataset.py:491`). قبل از اعتماد به هر اجرای organ-loss،
  `scripts/check_seg_masks.py` را اجرا کنید.
- **`SEG_SUFFIX` باید `'_seg_full'` بماند** — `_seg_reg` قرارداد label متفاوتی دارد.
- **rsync خروجی مدام تودرتو می‌شود.** مقصد نسبی، دو بار
  `out_synthesis_train/out_synthesis_train/` و یک بار یک clone از خود مخزن ساخته.
  حتماً از مسیر مقصد **مطلق** استفاده کنید.

---

## ۱۱. شکل نهایی رساله

نقطهٔ قوت این کار یک عدد بهتر نیست — **روش‌شناسی سخت‌گیرانه در برابر نتایج منفی**
است: کف نویز اندازه‌گیری‌شده‌ای که ادعای اصلی خودِ پروژه را رد کرد، آزمون‌های زوجی
per-case، و چهار اندازه‌گیری مستقل که به یک تشخیص واحد همگرا شدند.

> سنتز NCCT→CECT روی حدود ۸ HU خطای per-organ متوقف می‌شود. نشان می‌دهیم این
> نه overfitting است، نه registration، نه مقیاس voxel و نه ظرفیت — و آن را به
> شرطی‌سازی مدل‌نشدهٔ سطح کنتراست نسبت می‌دهیم. سپس نشان می‌دهیم شرطی‌سازی صریح
> (و کمّی‌سازی باقی‌ماندهٔ تقلیل‌ناپذیر) بخشی از آن را بازمی‌گرداند.

این خیلی قوی‌تر از «یک شبکهٔ بزرگ‌تر امتحان کردیم» است. **خودِ تشخیص، مشارکت علمی
است.**
